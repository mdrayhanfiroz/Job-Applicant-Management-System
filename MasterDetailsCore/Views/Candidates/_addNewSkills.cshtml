@model masterdetailsCore.Models.Skill

<div class="row mb-2 d-flex justify-content-between">
    <div class="col-10">
        
        @Html.DropDownListFor(x => x.SkillId, ViewBag.skill as SelectList, "-select-", new {@class="form-control mb-2"})
    </div>
    <div class="col-2">
        <a id="deleteSkill" class="btn btn-danger btn-sm"><i class="fa fa-trash"></i></a>
    </div>
</div>